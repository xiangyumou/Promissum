services:
  app:
    # Use the image built by GitHub Actions
    # Note: Make sure the repository name matches (lowercase recommended for docker)
    image: ghcr.io/xiangyumou/promissum:latest
    # Disable build in production to force pulling the image
    build: !reset {}
    restart: always
    environment:
      - NODE_ENV=production
